class System{

    constructor(){}

    // Add code to authenticate the given code and the access codes.
    authenticate(actualCode,enterdedCode){
        if(actualCode===enterdedCode)
        return  true
        else
        return false
        
        }
    

}